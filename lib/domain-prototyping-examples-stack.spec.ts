// import * as cdk from 'aws-cdk-lib/core';
// import { Template } from 'aws-cdk-lib/assertions';
// import DomainPrototypingExamplesStack from './domain-prototyping-examples-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/domain-prototyping-examples-stack.ts
it('Should create an SQS Queue', () => {
//   const app = new cdk.App();
//   const stack = new DomainPrototypingExamplesStack(app, 'ExamplesStack');
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
